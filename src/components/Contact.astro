<section id='contact' class='section'>
  <div class='container'>
    <h2 class='section-title'>Get In Touch</h2>
    <p class='section-subtitle'>
      While I’m not exploring new roles right now, I'm always open to meaningful
      conversations. Feel free to reach out. I'm always happy to connect and
      hear new projects.
    </p>
    <a
      class='btn btn-primary'
      href='mailto:aandradeb99@gmail.com'
      rel='noopener noreferrer'
      target='_blank'>Say Hello</a
    >
  </div>
</section>

<style>
  .section-title {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }

  .section-subtitle {
    font-size: 1.125rem;
    color: var(--color-text-secondary);
    margin-bottom: 3rem;
  }

  label {
    font-size: 0.875rem;
    font-weight: 500;
  }

  input,
  textarea {
    background-color: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 4px;
    padding: 0.75rem;
    color: var(--color-text);
    font-family: inherit;
    font-size: 1rem;
    transition: border-color 0.2s ease;
  }

  input:focus,
  textarea:focus {
    outline: none;
    border-color: var(--color-accent);
  }

  button {
    align-self: flex-start;
  }
</style>
