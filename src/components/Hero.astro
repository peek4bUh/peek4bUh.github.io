<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1>Hi, I'm <span class="highlight">Angel Andrade</span></h1>
      <h2>Full Stack Developer & Hacker Enthusiast</h2>
      <p>I build exceptional digital experiences with clean, efficient code.</p>
      <div class="hero-buttons">
        <a href="#projects" class="btn btn-primary">View My Work</a>
        <a href="#contact" class="btn">Contact Me</a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: calc(100vh - 5rem);
    display: flex;
    align-items: center;
    padding: 2rem 0;
    border-bottom: 1px solid var(--color-border);
  }

  .container {
    z-index: 5;
  }

  .hero-content {
    max-width: 800px;
  }

  h1 {
    font-size: 3.5rem;
    margin-bottom: 1rem;
    line-height: 1.1;
  }

  .highlight {
    color: var(--color-accent);
  }

  h2 {
    font-size: 1.75rem;
    color: var(--color-text-secondary);
    margin-bottom: 1.5rem;
    font-weight: 400;
  }

  p {
    font-size: 1.25rem;
    margin-bottom: 2rem;
    color: var(--color-text-secondary);
    max-width: 600px;
  }

  .hero-buttons {
    display: flex;
    gap: 1rem;
  }

  @media (max-width: 768px) {
    h1 {
      font-size: 2.5rem;
    }

    h2 {
      font-size: 1.5rem;
    }

    p {
      font-size: 1.125rem;
    }
  }
</style>
